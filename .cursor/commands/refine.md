# /refine — Prompt clarity

Refine vague user intent into a precise task, then execute.

## Default

1. Load `.cursor/skills/refine/SKILL.md`
2. Follow the refine workflow (context → clarify if needed → refined prompt → execute)
3. Reply in the user's language
